# ===================================================================
#  Kubernetes Secrets - Environment Variables
# ===================================================================
# ⚠️  IMPORTANT: Replace placeholder values with your actual secrets
#     Never commit real secrets to version control!
# ===================================================================

apiVersion: v1
kind: Secret
metadata:
  name: hierarchical-search-secrets
  namespace: openwebui-epfl
type: Opaque
stringData:
  # -------------------------------------------
  # RCP API (Embeddings & LLM)
  # -------------------------------------------
  RCP_API_ENDPOINT: "https://inference.rcp.epfl.ch/v1"
  RCP_API_KEY: "your-rcp-api-key-here"
  RCP_QWEN_EMBEDDING_MODEL: "Qwen/Qwen3-Embedding-8B"

  # -------------------------------------------
  # Reranking Model
  # -------------------------------------------
  RERANK_MODEL: "BAAI/bge-reranker-v2-m3"

  # -------------------------------------------
  # Internal API Security
  # -------------------------------------------
  INTERNAL_API_KEY: "your-secure-internal-api-key-here"

  # -------------------------------------------
  # ServiceNow Configuration
  # -------------------------------------------
  SERVICENOW_URL: "https://epfl.service-now.com"
  SERVICENOW_USERNAME: "WS_AI"
  SERVICENOW_KEY: "your-servicenow-api-key-here"
  
  # List of Knowledge Base IDs (comma-separated, no spaces)
  SERVICENOW_KB_IDS_FINANCE: "kb_id_1,kb_id_2,kb_id_3"